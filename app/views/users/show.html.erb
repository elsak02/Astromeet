<div class="container">
  <div class="profile-card">
    <div class="card-top">
      <h1><%= @user.first_name %> <%= @user.last_name %></h1>
    </div>
    <div class="card-bottom">
      <p> Date of birth: <%= @user.birth_date %></p>
      <p> Time of birth: <%= @user.birth_time %></p>
      <p> Place of birth: <%= @user.birth_place %></p>
    </div>
  </div>
</div>
